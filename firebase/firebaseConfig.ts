import { initializeApp } from 'firebase/app';

const firebaseConfig = {
  // apiKey: 'to be added',
  // authDomain: 'to be added',
  // projectId: 'to be added',
  // storageBucket: 'to be added',
  // messagingSenderId: 'to be added',
  // appId: 'to be added',
};

const app = initializeApp(firebaseConfig);

export default app;